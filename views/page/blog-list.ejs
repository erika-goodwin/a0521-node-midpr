<%- include('../includes/head.ejs') %>
  <body class="h-full">
    <div>
        <%- include('../includes/nav.ejs') %>
        <main class="flex-1">
          <div class="py-6">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
              <h1 class="text-2xl font-semibold text-gray-900">Dashboard</h1>
            </div>
            <div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
              <!-- Replace with your content -->
              <% if (blogs.length > 0) { %>
                <% for (let blog of blogs) { %>
                  <div class="py-4">
                    <div class="border-4 border-dashed border-gray-200 rounded-lg h-96">
    
                        <h2><%= blog.title %></h2>
                        <p><%= blog.content %></p>
                        <a href="/api/blogs/edit/<%= blog._id %>?edit=true">Edit</a>
                        <!-- <a href="/api/blogs/delete/<%= blogs._id %>">Delete</a> -->
                        <form action="/api/blogs/delete/<%= blog._id %>" method="post"><button type="submit">Delete</button></form>
                      
                        <!-- <button type="button" class="  inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                          Edit
                        </button>
                        <button type="button" class="  inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                          Deleted
                        </button> -->
    
                    </div>
                  </div>
                  
                <% } %>
              <% } %>
              <!-- /End replace -->
            </div>
          </div>
        </main>
      </div>
    </div>
<%- include('../includes/end.ejs') %>